filebeat.inputs:
  - type: log
    enabled: true
    paths:
      - /usr/share/filebeat/logs/notification-service-log.log

    start_position: beginning
    sincedb_path: /dev/null

output.logstash:
  hosts: ["${logstash_ip}:5044"]
